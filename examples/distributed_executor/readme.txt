a plain remote executor service. Note that a pull-work model is used instead of a "push work" like standard java
executors. Ofc its possible to also implement a distributed executor model using the push model.
